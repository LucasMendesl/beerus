package version

// Informations came from build flags.
var (
	// Version is the semantic version of the Beerus release.
	Version = "unknown"

	// GitCommit is the SHA of the commit that the binary was built from.
	GitCommit = "unknown"

	// Date is the date when the binary was built.
	ReleaseDate = "unknown"
)
